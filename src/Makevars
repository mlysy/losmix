.PHONY: all tmblib

all: $(SHLIB)
$(SHLIB): tmblib

tmblib: 
	(cd TMB; $(R_HOME)/bin$(R_ARCH_BIN)/Rscript --no-save --no-restore compile.R '-I"../../inst/include"')

clean:
	rm -rf *.so *.o TMB/*.so TMB/*.o
